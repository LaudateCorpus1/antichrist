// Blame the fucker who maintains the cryptojs GitHub repo
// It’s an abomination
var exports = {};
var util = {}

define(['lib/SHA1', 'lib/HMAC', 'lib/AES'], function(Crypto) {
	return Crypto
});

